% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_hei_gridded.R
\name{create_hei_gridded}
\alias{create_hei_gridded}
\title{Create a State of Global Air-style gridded map}
\usage{
create_hei_gridded(
  gridded_data,
  max_global_cells = 5e+06,
  max_inset_cells = 5e+06,
  poly_data,
  watermark = TRUE,
  watermark_text = "State of Global Air",
  watermark_x = -Inf,
  watermark_y = Inf,
  palette,
  trans = "identity",
  breaks,
  cut_data = TRUE,
  legend_title,
  legend_labels,
  legend_labels_format = "standard",
  legend_blank_col,
  legend_pos_x = 0,
  legend_pos_y = 0,
  disputed = FALSE,
  disputed_boundaries = NULL,
  disp_fil,
  disp_col,
  bord_col,
  insets = FALSE,
  plot_units = "px",
  plot_width = 2600,
  plot_height = 1450,
  plot_dps = 300,
  plot_font = "PT Sans",
  plot_legend_title_size = 9,
  plot_legend_text_size = 9,
  plot_text_size = 9,
  plot_key_size = 0.6,
  plot_key_spacing = 0.2,
  plot_legend_background = "#FFFFFF",
  save_plot = TRUE,
  out_path,
  out_file_name,
  out_types = c("png", "jpg", "pdf", "eps"),
  info_source,
  info_year,
  info_poll,
  info_data
)
}
\arguments{
\item{gridded_data}{A `terra::SpatRaster` containing the gridded values to
plot. For `cut_data = TRUE`, pass a classified raster whose values map to
the requested discrete legend labels. For `cut_data = FALSE`, pass a
continuous raster.}

\item{max_global_cells}{Maximum number of raster cells to draw in the main
map. Passed to `tidyterra::geom_spatraster(maxcell = ...)`.}

\item{max_inset_cells}{Maximum number of raster cells intended for inset
maps. This argument is currently reserved and is not used by the function.}

\item{poly_data}{An `sf` object containing boundary polygons. The object must
include a `loc_name` column because the function uses it to fill selected
disputed areas.}

\item{watermark}{Logical. If `TRUE`, also creates watermarked saved output
when `save_plot = TRUE`. The current implementation expects this to remain
`TRUE`.}

\item{watermark_text}{Character string used as the watermark label.}

\item{watermark_x, watermark_y}{Numeric watermark coordinates. These arguments
are currently reserved; the watermark is drawn in the lower-left corner.}

\item{palette}{Character vector of colors. For `cut_data = TRUE`, colors are
used as a discrete scale. For `cut_data = FALSE`, colors are used in a
continuous gradient.}

\item{trans}{Character string or transformation object passed to
`ggplot2::scale_fill_gradientn()` when `cut_data = FALSE`.}

\item{breaks}{Numeric vector of legend breaks. These are also named with
`legend_labels` internally.}

\item{cut_data}{Logical. If `TRUE`, draw the raster with a discrete fill
scale. If `FALSE`, draw it with a continuous gradient fill scale.}

\item{legend_title}{Character string used as the fill legend title. Markdown
is supported because legend titles are rendered with
`ggtext::element_markdown()`.}

\item{legend_labels}{Character vector of labels shown in the legend.}

\item{legend_labels_format}{Character string controlling legend label
rendering. Use `"markdown"` for markdown labels; any other value uses
plain text labels.}

\item{legend_blank_col}{Color used to fill the final legend key when the last
entry is intended to be text-only.}

\item{legend_pos_x, legend_pos_y}{Numeric legend position coordinates in the
range used by `ggplot2::theme(legend.position = c(x, y))`.}

\item{disputed}{Logical. If `TRUE`, draw disputed boundaries from
`disputed_boundaries`.}

\item{disputed_boundaries}{An `sf` object containing disputed boundary line
or polygon features. Required when `disputed = TRUE`.}

\item{disp_fil}{Fill color used for selected disputed or special-status
polygon areas, currently French Guiana and Western Sahara when present in
`poly_data$loc_name`.}

\item{disp_col}{Line color used when drawing `disputed_boundaries`.}

\item{bord_col}{Boundary line color for `poly_data`.}

\item{insets}{Logical flag intended for inset maps. This argument is
currently reserved and is not used by the function.}

\item{plot_units}{Units passed to `ggplot2::ggsave()`.}

\item{plot_width, plot_height}{Plot dimensions passed to `ggplot2::ggsave()`.}

\item{plot_dps}{Plot resolution, in dots per inch, passed to
`ggplot2::ggsave(dpi = ...)`.}

\item{plot_font}{Font family used in plot text.}

\item{plot_legend_title_size, plot_legend_text_size, plot_text_size}{Numeric
font sizes for legend titles, legend labels, and general plot text.}

\item{plot_key_size, plot_key_spacing}{Numeric legend key size and vertical
spacing, in centimeters.}

\item{plot_legend_background}{Fill color for the legend background.}

\item{save_plot}{Logical. If `TRUE`, save plot files and a metadata text file
to `out_path`. If `FALSE`, return the unwatermarked `ggplot` object.}

\item{out_path}{Character path prefix where output files are written when
`save_plot = TRUE`. Include a trailing path separator if needed, because
filenames are built with `paste0(out_path, out_file_name, ...)`.}

\item{out_file_name}{Character file stem used for saved plot and metadata
outputs.}

\item{out_types}{Character vector of output formats to save. Supported values
are `"png"`, `"jpg"`, `"pdf"`, and `"eps"`.}

\item{info_source, info_year, info_poll, info_data}{Metadata values written to
the companion text file when `save_plot = TRUE`.}
}
\value{
If `save_plot = FALSE`, a `ggplot` object. If `save_plot = TRUE`, the
  function is called for its side effects: plot files and a metadata text file
  are written to disk and no value is returned.
}
\description{
Builds a global gridded map from a `terra` raster and `sf` polygon
boundaries, applies HEI/State of Global Air styling, and optionally saves the
resulting plot in one or more file formats. When `save_plot = FALSE`, the
function returns the unwatermarked `ggplot` object so it can be inspected,
modified, or saved by the caller.
}
\examples{
if (requireNamespace("sf", quietly = TRUE) &&
    requireNamespace("terra", quietly = TRUE)) {
  r <- terra::rast(
    nrows = 2,
    ncols = 2,
    xmin = 0,
    xmax = 2,
    ymin = 0,
    ymax = 2,
    vals = c(0.2, 0.4, 0.6, 0.8)
  )

  boundary <- sf::st_as_sf(
    data.frame(
      loc_name = "Example",
      wkt = "POLYGON ((0 0, 2 0, 2 2, 0 2, 0 0))"
    ),
    wkt = "wkt",
    crs = 4326
  )

  p <- create_hei_gridded(
    gridded_data = r,
    poly_data = boundary,
    palette = c("#216999", "#d96354"),
    breaks = c(0, 1),
    cut_data = FALSE,
    legend_title = "Example value",
    legend_labels = c("Low", "High"),
    legend_blank_col = "white",
    bord_col = "gray40",
    disp_fil = "gray90",
    disp_col = "gray40",
    save_plot = FALSE
  )
  p
}

}
